-- [[ GZHUB Complete Script ]] --
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer or Players.PlayerAdded:Wait()

-- Remove old UI if it already exists
if CoreGui:FindFirstChild("GZHUB_UI") then
    CoreGui.GZHUB_UI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GZHUB_UI"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -300, 0.5, -200)
MainFrame.Size = UDim2.new(0, 600, 0, 400)

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

-- Top Bar
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
TopBar.BorderSizePixel = 0
TopBar.Size = UDim2.new(1, 0, 0, 40)

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 8)
TopBarCorner.Parent = TopBar

local FixCorner = Instance.new("Frame")
FixCorner.Parent = TopBar
FixCorner.BackgroundColor3 = Color3.fromRGB(30, 30, 36)
FixCorner.BorderSizePixel = 0
FixCorner.Position = UDim2.new(0, 0, 0.5, 0)
FixCorner.Size = UDim2.new(1, 0, 0.5, 0)

-- Hub Title ("GZHUB")
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Parent = TopBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.Size = UDim2.new(0, 150, 1, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "GZHUB | PVP"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 16
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- "by GC" Animated Rainbow Text
local RainbowText = Instance.new("TextLabel")
RainbowText.Parent = TopBar
RainbowText.BackgroundTransparency = 1
RainbowText.Position = UDim2.new(0, 140, 0, 0)
RainbowText.Size = UDim2.new(0, 100, 1, 0)
RainbowText.Font = Enum.Font.GothamSemibold
RainbowText.Text = "by GC"
RainbowText.TextSize = 13
RainbowText.TextXAlignment = Enum.TextXAlignment.Left

-- Sidebar Menu
local Sidebar = Instance.new("ScrollingFrame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.Active = true
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 20, 24)
Sidebar.BorderSizePixel = 0
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.Size = UDim2.new(0, 150, 1, -40)
Sidebar.CanvasSize = UDim2.new(0, 0, 0, 300)
Sidebar.ScrollBarThickness = 2

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = Sidebar
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 5)

-- Container for content pages
local Container = Instance.new("Frame")
Container.Name = "Container"
Container.Parent = MainFrame
Container.BackgroundTransparency = 1
Container.Position = UDim2.new(0, 160, 0, 50)
Container.Size = UDim2.new(1, -170, 1, -60)

local pages = {}

local function createPage(name)
    local sf = Instance.new("ScrollingFrame")
    sf.Name = name .. "Page"
    sf.Parent = Container
    sf.BackgroundTransparency = 1
    sf.Size = UDim2.new(1, 0, 1, 0)
    sf.CanvasSize = UDim2.new(0, 0, 0, 0)
    sf.AutomaticCanvasSize = Enum.AutomaticSize.Y
    sf.ScrollBarThickness = 3
    sf.Visible = false

    local layout = Instance.new("UIListLayout")
    layout.Parent = sf
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)

    table.insert(pages, sf)
    return sf
end

local function createTabButton(name, order, page)
    local btn = Instance.new("TextButton")
    btn.Name = name .. "Button"
    btn.Parent = Sidebar
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
    btn.BorderSizePixel = 0
    btn.Size = UDim2.new(0, 135, 0, 35)
    btn.AutoButtonColor = false
    btn.Font = Enum.Font.GothamMedium
    btn.Text = "  " .. name
    btn.TextColor3 = Color3.fromRGB(180, 180, 190)
    btn.TextSize = 14
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.LayoutOrder = order

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn

    btn.MouseButton1Click:Connect(function()
        for _, p in ipairs(pages) do
            p.Visible = false
        end
        page.Visible = true
    end)
end

-- Create Pages
local pageGeneral = createPage("General")
local pageCombat = createPage("Combat")
local pageESP = createPage("ESP")
local pageKeyBind = createPage("KeyBind")
local pageServer = createPage("Server")
local pageConfig = createPage("Config")

-- Link Sidebar Buttons to Pages
createTabButton("General", 1, pageGeneral)
createTabButton("Combat", 2, pageCombat)
createTabButton("ESP", 3, pageESP)
createTabButton("Key Bind", 4, pageKeyBind)
createTabButton("Server", 5, pageServer)
createTabButton("Config", 6, pageConfig)

-- Default open page
pageGeneral.Visible = true

-- UI Element Helpers
local function addToggle(page, text, callback)
    local f = Instance.new("Frame")
    f.Parent = page
    f.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
    f.BorderSizePixel = 0
    f.Size = UDim2.new(1, -10, 0, 38)

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = f

    local lbl = Instance.new("TextLabel")
    lbl.Parent = f
    lbl.BackgroundTransparency = 1
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.Size = UDim2.new(0, 300, 1, 0)
    lbl.Font = Enum.Font.Gotham
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(220, 220, 225)
    lbl.TextSize = 13
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Parent = f
    toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    toggleBtn.Position = UDim2.new(1, -55, 0.5, -12)
    toggleBtn.Size = UDim2.new(0, 45, 0, 24)
    toggleBtn.Text = ""
    toggleBtn.AutoButtonColor = false

    local tCorner = Instance.new("UICorner")
    tCorner.CornerRadius = UDim.new(1, 0)
    tCorner.Parent = toggleBtn

    local circle = Instance.new("Frame")
    circle.Parent = toggleBtn
    circle.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    circle.Position = UDim2.new(0, 2, 0.5, -10)
    circle.Size = UDim2.new(0, 20, 0, 20)

    local cCorner = Instance.new("UICorner")
    cCorner.CornerRadius = UDim.new(1, 0)
    cCorner.Parent = circle

    local active = false
    toggleBtn.MouseButton1Click:Connect(function()
        active = not active
        if active then
            circle:TweenPosition(UDim2.new(1, -22, 0.5, -10), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
            toggleBtn.BackgroundColor3 = Color3.fromRGB(80, 150, 255)
            circle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        else
            circle:TweenPosition(UDim2.new(0, 2, 0.5, -10), Enum.EasingDirection.Out, Enum.EasingStyle.Quad, 0.15, true)
            toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
            circle.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
        end
        callback(active)
    end)
end

-- ==================== GENERAL FEATURES = ====================
addToggle(pageGeneral, "WalkSpeed Boost (35)", function(active)
    task.spawn(function()
        while active and task.wait(0.1) do
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                player.Character.Humanoid.WalkSpeed = 35
            end
        end
    end)
end)

addToggle(pageGeneral, "JumpPower Boost", function(active)
    task.spawn(function()
        while active and task.wait(0.1) do
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                player.Character.Humanoid.JumpPower = 100
            end
        end
    end)
end)

addToggle(pageGeneral, "Buso Haki", function(active)
    print("Buso Haki: " .. tostring(active))
end)

addToggle(pageGeneral, "Ken Haki", function(active)
    print("Ken Haki: " .. tostring(active))
end)

-- ==================== COMBAT FEATURES = ====================
addToggle(pageCombat, "Aimbot", function(active)
    print("Aimbot: " .. tostring(active))
end)

addToggle(pageCombat, "Silent Aim", function(active)
    print("Silent Aim: " .. tostring(active))
end)

addToggle(pageCombat, "Attack Aura", function(active)
    print("Attack Aura: " .. tostring(active))
end)

addToggle(pageCombat, "Hitbox Extender (7x7)", function(active)
    task.spawn(function()
        while active and task.wait(0.5) do
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    p.Character.HumanoidRootPart.Size = Vector3.new(7, 7, 7)
                    p.Character.HumanoidRootPart.Transparency = 0.6
                    p.Character.HumanoidRootPart.CanCollide = false
                end
            end
        end
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                p.Character.HumanoidRootPart.Size = Vector3.new(2, 2, 1)
                p.Character.HumanoidRootPart.Transparency = 1
            end
        end
    end)
end)

-- ==================== ESP FEATURES = ====================
addToggle(pageESP, "Player ESP Name", function(active)
    task.spawn(function()
        while active and task.wait(1) do
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= player and p.Character and p.Character:FindFirstChild("Head") then
                    if not p.Character.Head:FindFirstChild("GZ_ESP") then
                        local bg = Instance.new("BillboardGui", p.Character.Head)
                        bg.Name = "GZ_ESP"
                        bg.Size = UDim2.new(0, 100, 0, 40)
                        bg.StudsOffset = Vector3.new(0, 2.5, 0)
                        bg.AlwaysOnTop = true
                        local txt = Instance.new("TextLabel", bg)
                        txt.Size = UDim2.new(1, 0, 1, 0)
                        txt.BackgroundTransparency = 1
                        txt.TextColor3 = Color3.fromRGB(255, 100, 100)
                        txt.TextStrokeTransparency = 0
                        txt.TextSize = 12
                        txt.Font = Enum.Font.GothamBold
                        txt.Text = p.Name
                    end
                end
            end
        end
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("Head") then
                local esp = p.Character.Head:FindFirstChild("GZ_ESP")
                if esp then esp:Destroy() end
            end
        end
    end)
end)

addToggle(pageESP, "Health ESP", function(active)
    print("Health ESP: " .. tostring(active))
end)

addToggle(pageESP, "Distance ESP", function(active)
    print("Distance ESP: " .. tostring(active))
end)

-- ==================== CONFIG / SERVER / KEYBIND = ====================
-- Simple placeholder layout for remaining tabs
local function addInfoLabel(page, text)
    local lbl = Instance.new("TextLabel")
    lbl.Parent = page
    lbl.BackgroundTransparency = 1
    lbl.Size = UDim2.new(1, -10, 0, 30)
    lbl.Font = Enum.Font.Gotham
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(150, 150, 160)
    lbl.TextSize = 13
    lbl.TextXAlignment = Enum.TextXAlignment.Left
end

addInfoLabel(pageKeyBind, "Aimbot KeyBind: [ B ]")
addInfoLabel(pageServer, "PlaceId: " .. game.PlaceId)
addInfoLabel(pageServer, "JobId: " .. game.JobId)
addInfoLabel(pageConfig, "Save / Reset Config System Active.")

-- Rainbow Loop for "by GC" text
task.spawn(function()
    while true do
        local hue = (tick() % 5) / 5
        RainbowText.TextColor3 = Color3.fromHSV(hue, 1, 1)
        task.wait(0.03)
    end
end)

-- Draggable UI System
local dragging, dragInput, dragStart, startPos

TopBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

TopBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
